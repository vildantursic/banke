import { FilterNewsPipe } from './filter-news.pipe';

describe('FilterNewsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNewsPipe();
    expect(pipe).toBeTruthy();
  });
});
